---
'posthog': minor
'posthog-android': minor
'posthog-server': minor
---

Add the internal `persistOptOut` config so a wrapper SDK that owns consent can stop a persisted opt-out from overriding the value it passes to setup.
